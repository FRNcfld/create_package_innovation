# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false


## Environment Properties

# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=1.20.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.20.1,1.21)
# The Forge version must agree with the Minecraft version to get a valid artifact
forge_version=47.4.10
# The Forge version range can use any version of Forge as bounds or match the loader version range
forge_version_range=[47,)
# The loader version range can only use the major version of Forge/FML as bounds
loader_version_range=[47,)
# The mapping channel to use for mappings.
# The default set of supported mapping channels are ["official", "snapshot", "snapshot_nodoc", "stable", "stable_nodoc"].
# Additional mapping channels can be registered through the "channelProviders" extension in a Gradle plugin.
#
# | Channel   | Version              |                                                                                |
# |-----------|----------------------|--------------------------------------------------------------------------------|
# | official  | MCVersion            | Official field/method names from Mojang mapping files                          |
# | parchment | YYYY.MM.DD-MCVersion | Open community-sourced parameter names and javadocs layered on top of official |
#
# You must be aware of the Mojang license when using the 'official' or 'parchment' mappings.
# See more information here: https://github.com/MinecraftForge/MCPConfig/blob/master/Mojang.md
#
# Parchment is an unofficial project maintained by ParchmentMC, separate from Minecraft Forge.
# Additional setup is needed in order to use their mappings, see https://parchmentmc.org/docs/getting-started
mapping_channel=parchment
# The mapping version to query from the mapping channel.
# This must match the format required by the mapping channel.
# 机械动力 6.0.x 使用 Parchment 2023.08.20 构建，附属模组沿用同一版本，
# 否则 Create 的 mixin refmap 在开发环境中无法正确应用。
mapping_version=2023.08.20-1.20.1


## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=create_package_innovation
# The human-readable display name for the mod.
mod_name=Create Package Innovation
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=1.0.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.frnc.create_package_innovation
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=FRNc
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
# 换行写成 \n（properties 文件规范），processResources 展开后会成为 mods.toml 里的多行字面量。
# 注意：这个值会被 Groovy 模板引擎展开，正文里不能出现美元符号紧跟大括号的写法。
mod_description=让同一个容器上的多台打包机/理包机并行发送同一张订单：整理好的包裹进入按容器分桶的共享池，空闲机器各自取包发货，N 台 ≈ N 倍速度。\n支持所有容器——Create 保险库与流体罐、Create: Connected 纵向保险库、Create: Storage 网络，以及其他模组的任意储物方块；Create: FluidLogistics 的流体打包机同样适用。\n共享池存在存档里：拆机器不爆池，容器被拆时才把包裹如数爆出，漏抽的池连重启之后也能回收。


## 依赖版本

# ---------------- 机械动力 ----------------
# 1.20.1 的维护线是 Create 6.0.x；Ponder / Flywheel / Registrate 是 :slim 包不带的内嵌依赖。
create_version=6.0.8-289
ponder_version=1.0.91
flywheel_version=1.0.5
registrate_version=MC1.20-1.3.3

# ---------------- Create 附属模组 ----------------
# 这三个都只在 Modrinth 发布，仓库见 build.gradle 里的 exclusiveContent。
# 版本可以填 Modrinth 版本页上的版本号，也可以填版本 ID。
create_connected_version=1.2.3-mc1.20.1
fluidlogistics_version=1.3.0-mc1.20.1
# Create: Storage 用版本 ID：它的版本号 1.2.7 在 Forge(1.20.1) 与 NeoForge(1.21.1)
# 两个构建上完全相同，Modrinth 仓库按版本号无法区分。
# oSsfZYxj 对应 1.2.7（1.20.1 Forge）；换版本时从 Modrinth 版本页 URL 末段取新 ID。
create_storage_version=oSsfZYxj

# ---------------- JEI ----------------
jei_version=15.56.0.205
